#!/usr/bin/env node

/**
 * Foundation Remove - Comando Global
 * Script de conveniência para executar o foundation-uninstaller
 */

const fs = require('fs');
const path = require('path');
const { spawn } = require('child_process');

const foundationDir = __dirname;
const uninstallerScript = path.join(foundationDir, 'foundation-uninstaller.cjs');

// Verifica se o script existe
if (!fs.existsSync(uninstallerScript)) {
  console.log('❌ Foundation Uninstaller não encontrado');
  console.log('💡 Certifique-se de que a pasta foundation/ existe');
  process.exit(1);
}

// Executa o script de desinstalação
console.log('🗑️  Executando Foundation Uninstaller...\n');

const child = spawn('node', [uninstallerScript], {
  stdio: 'inherit',
  cwd: foundationDir
});

child.on('close', (code) => {
  process.exit(code);
});

child.on('error', (error) => {
  console.error('❌ Erro ao executar Foundation Remover:', error.message);
  process.exit(1);
});